import * as React from "react"
import Layout from '../components/layout'

// markup
export default function ProjectsPage () {
    return (
        <Layout>
            <p>test</p>
        </Layout>
    );
};